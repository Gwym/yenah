
export interface EpigeneticsOptions {
    inExtension: '.ts'
    outExtension: '.gen.ts'
    inRootPath: string
    inFiles: string[]
    outFilename: string
}

export class EpiGen {

    genFrontEnd(opt: EpigeneticsOptions) {
        // TODO (0) : epigen


       // refrencepath ??
    }

}