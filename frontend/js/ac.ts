let universe_representation = {

  /*  getEntityType: function(entity) {
        
        flattenEntity(); 
        // { attribute: {adjective : value}} => { attribute_adjective: value }
        decomposeEntity(); 
        // { attribute_adjective: value } => { attribute: {adjective : value}}
        // ex : move_earth: => move.earth
        
        'furniture', // !move && !conscience && !perception => disable all but entity_attributes ?
        'seed', // = furniture + "program for plant" (information of composition)
        'egg', // = furniture + "program for being"
        'workbench', // living === false; => cycle decomposition
        'building', // one cell furniture  ?
        'plants', // !move living === true; => cycle growth + decomposition
        'being', // move & living
        'stone', 'wood'
        
        // TODO (4) : decomposition and dissasembly cycles
        {
          genetic: {}
        }
        
        // To build an artifact, it must be part of a cycle 
        
        // Example for the workbench : ~ métier (carried by Entity.genetic.*)
        stone planck = wood + handsaw
        
    }*/
}

let souvenirs = {
  events: [],
  entities: [],
  cells: [],
  images: [], // symbolic representations
  furnitures: [],
  workbench: [],
  building: [],
  plants: [],
  being: [],
  place: []
  // short term

  // middle term

  // long term
}
